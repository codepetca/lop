/**
 * Application-wide constants
 */

/** Maximum number of polls to store in localStorage */
export const MAX_SAVED_POLLS = 10;

/** Duration to show copy feedback message (ms) */
export const COPY_FEEDBACK_TIMEOUT = 2000;

/** Minimum members per group */
export const MIN_MEMBERS_PER_GROUP = 1;

/** Maximum members per group */
export const MAX_MEMBERS_PER_GROUP = 10;
